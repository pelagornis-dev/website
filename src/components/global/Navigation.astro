---
import Pelagornis from '../../../public/logo/pelagornis.svg';

import { useTranslations, getLangFromUrl, useTranslatedPath, getLangFromPath } from "@i18n/utils";
import {ui as languages} from "@i18n/ui"
const { lang, url } = Astro.props;
const t = useTranslations(lang);
const translatedPath = useTranslatedPath(lang);

---
<header
  aria-labelledby="navigation"
  id="navigation"
  class="top-0 relative w-full z-50 border-b border-white/5 backdrop-blur-2xl"
>
  <div class="relative flex flex col w-full py-3 px-3 md:items-center md:justify-between md:flex-row md:px:8 lg:px:12">
    <div class="flex items-center" class="flex items-center">
      <a href="/" title="Logo">
        <Pelagornis width={129} height={20}/>
      </a>
    </div>
  </div>
</header>